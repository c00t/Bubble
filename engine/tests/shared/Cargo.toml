[package]
name = "shared"
version = "0.1.0"
edition = "2021"

[dependencies]
async-ffi = { version = "0.5.0", features = ["macros"] }
bubble-core = {path = "../../app/bubble_core", default_features = false}
bubble-tasks = {workspace = true}
